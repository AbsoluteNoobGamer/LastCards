{
  "name": "cards-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --only firestore --project demo-cards-firestore-rules \"node --test firestore-tests/firestore.rules.test.mjs\"",
    "deploy:firestore-rules": "firebase deploy --only firestore:rules --project lastcards-d4396"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.6.0",
    "firebase-tools": "^13.29.1"
  }
}
